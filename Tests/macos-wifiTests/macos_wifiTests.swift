import XCTest
@testable import macos_wifi

final class macos_wifiTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(macos_wifi().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
